---

- name: zypper install package
  environment: "{{ env_vars }}"
  zypper:
    name: "{{ versioned_package }}"
    state: present
    oldpackage: yes
    update_cache: yes
    force: yes
  retries: 5
  delay: 60

...
